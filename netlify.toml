[build]
  publish = "."
  command = "echo 'No build needed - static HTML'"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
